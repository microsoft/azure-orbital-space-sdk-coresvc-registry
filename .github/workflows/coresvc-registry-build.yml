name: coresvc-fileserver-build

on: 
    workflow_dispatch:
    push:
        branches:
        - km/add_action

jobs:
  build-coresvc-fileserver-amd64:
    permissions:
      contents: read
      packages: write

    uses: microsoft/azure-orbital-space-sdk-github-actions/.github/worfklows/coresvc-build.yaml@km/add_core_workflow
    with:
      REGISTRY: ghcr.io
      APP_NAME: coresvc-registry
      ANNOTATION: azure-orbital-space-sdk-coresvc-registry.yaml
      WORKFLOW_AGENT: ubuntu-latest
      ARCHITECTURE: arm64
    secrets:
      GIT_HUB_USER_NAME: ${{ secrets.GIT_HUB_USER_NAME }}
      GIT_HUB_USER_TOKEN: ${{ secrets.GIT_HUB_USER_TOKEN }}
      SETUP_REPO_URL: ${{ secrets.SETUP_REPO_URL }}

  build-coresvc-fileserver-arm64:
    permissions:
      contents: read
      packages: write

    uses: microsoft/azure-orbital-space-sdk-github-actions/worfklows/coresvc-build.yaml@km/add_core_workflow
    with:
      REGISTRY: ghcr.io
      APP_NAME: coresvc-registry
      ANNOTATION: azure-orbital-space-sdk-coresvc-registry.yaml
      WORKFLOW_AGENT: ubuntu-latest
      ARCHITECTURE: amd64
    secrets:
      GIT_HUB_USER_NAME: ${{ secrets.GIT_HUB_USER_NAME }}
      GIT_HUB_USER_TOKEN: ${{ secrets.GIT_HUB_USER_TOKEN }}
      SETUP_REPO_URL: ${{ secrets.SETUP_REPO_URL }}